/**
 * @file
 * @brief CoAP frontend runtime — translation unit placeholder.
 *
 * Compiled only when `CONFIG_ZEPHLETS_COAP=y`. The frontend has no
 * runtime symbols yet; this TU exists so the library has a source to
 * link and downstream consumers can `#include <zephlet_coap_consts.h>`
 * from `frontends/coap/include/` without a separate library target.
 */
